---
phase: quick
plan: 260617-2om
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/system/system-boot.jsx
  - src/components/system/system-hero.jsx
  - src/components/system/system-loader.jsx
autonomous: true
requirements: [QUICK-FIX]

must_haves:
  truths:
    - "On page load, exactly one boot/loading visual is shown — never two overlays in sequence"
    - "HUD chrome (top bar, tour caption, hint, dock with nav + résumé buttons) is invisible and unclickable until the boot screen has fully faded out"
    - "Boot screen still shows the progressive '> initializing core…' / '> linking services [6/6]' / '> mounting database' / '> system online' lines, same timing as before"
    - "Clicking/pressing any key during boot still skips straight to fade-out, after which HUD appears"
    - "Reduced-motion users see the HUD immediately (never stuck waiting on a boot screen that's skipped)"
  artifacts:
    - path: "src/components/system/system-boot.jsx"
      provides: "Merged single boot overlay (SystemLoader chrome + BootSequence text), exports SystemBoot({ onDone })"
      contains: "export function SystemBoot"
    - path: "src/components/system/system-hero.jsx"
      provides: "Hero shell gating HUD chrome behind booted state, mounts SystemBoot as always-present sibling"
      contains: "SystemBoot"
  key_links:
    - from: "src/components/system/system-hero.jsx"
      to: "src/components/system/system-boot.jsx"
      via: "import { SystemBoot } and <SystemBoot onDone={() => setBooted(true)} />"
      pattern: "SystemBoot"
    - from: "src/components/system/system-hero.jsx"
      to: "booted state"
      via: "useState + useEffect gating HUD JSX behind {booted && (...)}"
      pattern: "booted &&"
---

<objective>
Fix the boot/loading sequence bug in the Career OS hero: two loading overlays (`SystemLoader` dynamic-import fallback, then inline `BootSequence`) currently flash in sequence while HUD chrome (top bar, tour caption, hint, dock) is already rendered and clickable underneath. Merge both overlays into a single `SystemBoot` component and gate all HUD chrome behind a `booted` flag that only flips true once the boot overlay has fully faded out.

Purpose: Eliminate the double-flash loading visual and the "clickable buttons under a loading screen" bug, without changing the design language already established (glass card, pulsing core, scanning bar, JetBrains Mono boot lines).
Output: New `src/components/system/system-boot.jsx`; updated `src/components/system/system-hero.jsx`; deleted `src/components/system/system-loader.jsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:\Projects\Portfolio\CLAUDE.md
@C:\Projects\Portfolio\src\components\system\system-hero.jsx
@C:\Projects\Portfolio\src\components\system\system-loader.jsx
@C:\Projects\Portfolio\src\data\identity.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create merged SystemBoot component</name>
  <files>src/components/system/system-boot.jsx</files>
  <action>
Create `src/components/system/system-boot.jsx` exporting `SystemBoot({ onDone })` as the single boot overlay, replacing both `SystemLoader` and the inline `BootSequence`.

Visual chrome — copy verbatim from `src/components/system/system-loader.jsx`: the `'use client'` directive, the `SR_ONLY` style object, the outer `role="status" aria-label="Loading interactive system"` wrapper at `position:absolute; inset:0; background:#020202`, the `@keyframes` block (`sl-scan`, `sl-pulse`, `sl-ring`; drop `sl-flick` since the flicker line is replaced), the faint background grid `div`, the glass card wrapper (`GLASS`-equivalent inline styles: `rgba(10,18,23,0.45)` background, `blur(22px) saturate(140%)`, border, boxShadow), the pulsing core ring/dot, and the `sourabh-jha://system` title line, the scanning progress bar, and the SEO `SR_ONLY` identity block (`identity.name` / `identity.title` / `identity.tagline` from `@/data/identity`).

Replace SystemLoader's static `initializing topology…` line (and drop the `sl-flick` keyframe) with the progressive boot-line reveal currently in system-hero.jsx's inline `BootSequence` (lines ~237-310): module-level constants `BOOT_LINES` (the 4 lines: `> initializing core…`, `> linking services [6/6]`, `> mounting database`, `> system online`), `LINE_DELAY_MS = 380`, `FADE_DELAY_MS = 200`, `FADE_DURATION = 0.35`. Inside `SystemBoot`, keep the `visibleCount` state + `setTimeout` cascade revealing one line every `LINE_DELAY_MS`, with the last line styled `color: '#eaf6fb'` + glow text-shadow and earlier lines `rgba(178,213,229,0.65)`. After the last line becomes visible, wait `FADE_DELAY_MS` then mark internal `done = true`.

Skip behavior: keep the `pointerdown`/`keydown` listeners registered with `{ once: true }` that immediately set `done = true` (skipping straight to fade-out) — same as the current `BootSequence`'s `skip` callback.

Exit signaling: wrap the whole overlay in Framer Motion `AnimatePresence` with `onExitComplete={onDone}` — `onDone` is the prop passed in from the parent, called exactly once, exactly when the fade-out (`exit={{ opacity: 0 }}`, `transition={{ duration: FADE_DURATION, ease: 'easeOut' }}`) finishes. This is the key behavioral change from the original `BootSequence` (which had no completion callback — its parent never knew when it finished).

The component must render the boot text lines INSIDE the same glass card as the pulsing core / title / progress bar (one merged visual), not as a second overlay layer. Keep the boot-lines block's container styling consistent with the rest of the card (JetBrains Mono font, same letter-spacing conventions used elsewhere in the file).

Do not import or reference `SystemLoader` — this file is fully self-contained and does not know about the 3D scene's load state.

Export both `SystemBoot` as a named export and as `export default SystemBoot` (matching the existing default-export convention in system-loader.jsx).
  </action>
  <verify>
    <automated>cd C:\Projects\Portfolio && node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/system/system-boot.jsx','utf8'); const assert=(c,m)=>{if(!c){console.error('FAIL: '+m);process.exit(1)}}; assert(s.includes('export function SystemBoot') || s.includes('export const SystemBoot'), 'SystemBoot exported'); assert(s.includes('onDone'), 'onDone prop used'); assert(s.includes('AnimatePresence'), 'AnimatePresence used'); assert(s.includes('onExitComplete'), 'onExitComplete wired'); assert(s.includes('BOOT_LINES'), 'boot lines present'); assert(s.includes('once: true'), 'skip listeners use once:true'); console.log('OK')"</automated>
  </verify>
  <done>`src/components/system/system-boot.jsx` exists, exports `SystemBoot({ onDone })`, contains the glass card chrome (pulsing core, title, scanning bar, SEO identity block) merged with the progressive BOOT_LINES reveal, uses `AnimatePresence` with `onExitComplete={onDone}`, and registers the same `{ once: true }` skip listeners.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SystemBoot into SystemHero, gate HUD chrome behind booted state, delete SystemLoader</name>
  <files>src/components/system/system-hero.jsx, src/components/system/system-loader.jsx</files>
  <action>
Edit `src/components/system/system-hero.jsx`:

1. Remove `import { SystemLoader } from './system-loader';`. Add `import { SystemBoot } from './system-boot';`.
2. Change the `dynamic(() => import('./system-scene'), { ssr: false, loading: () => <SystemLoader /> })` call to `dynamic(() => import('./system-scene'), { ssr: false })` — remove the `loading` option entirely so there is no second loading visual tied to chunk-load timing.
3. Delete the entire `/* ── Boot sequence ── */` block: the module-level constants `BOOT_LINES`, `LINE_DELAY_MS`, `FADE_DELAY_MS`, `FADE_DURATION`, and the `BootSequence` function definition (current lines ~237-310). This logic now lives entirely in `system-boot.jsx`.
4. Inside the `SystemHero` component (after the existing `const prefersReducedMotion = useReducedMotion();` line), add `const [booted, setBooted] = useState(false);`.
5. Add a `useEffect` immediately after that declares: if `prefersReducedMotion` is true, call `setBooted(true)` — this fires once on mount for reduced-motion users so `booted` never gets stuck `false` (since `SystemBoot` is skipped entirely on that path via the existing `{!prefersReducedMotion && ...}` guard). Dependency array: `[prefersReducedMotion]`.
6. In the returned JSX, replace `{!prefersReducedMotion && <BootSequence />}` with `{!prefersReducedMotion && !booted && <SystemBoot onDone={() => setBooted(true)} />}`.
7. Gate the following behind `{booted && ( ... )}` so none render or receive pointer events until boot has fully finished and faded out: the top HUD bar `<div className="pointer-events-none absolute inset-x-0 top-0 z-10 ...">` (containing `HudIdentity`, `HudControls`, `LiveTelemetry`), `<TourCaption active={tourActive} section={tourSection} />`, `<HudHint />`, and `<HudDock active={active} onSelect={selectSection} />`. Wrap these four elements in a single `{booted && ( <> ... </> )}` fragment, preserving their existing relative order and all existing props unchanged.
8. Leave `<CanvasOrFallback ... />`, `<SectionPanel ... />`, `<CommandPalette ... />`, and `<CommandTerminal ... />` exactly as-is — do not gate or move them.

Do not modify `system-scene.jsx`, `system-fallback.jsx`, or any data files.

After the edit, delete `src/components/system/system-loader.jsx` (already confirmed via grep that system-hero.jsx was its only importer — after step 1 above, zero remaining references exist in the codebase).
  </action>
  <verify>
    <automated>cd C:\Projects\Portfolio && node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/system/system-hero.jsx','utf8'); const assert=(c,m)=>{if(!c){console.error('FAIL: '+m);process.exit(1)}}; assert(!s.includes(\"from './system-loader'\"), 'system-loader import removed'); assert(s.includes(\"from './system-boot'\"), 'system-boot imported'); assert(!s.includes('function BootSequence'), 'BootSequence removed'); assert(s.includes('booted'), 'booted state present'); assert(!fs.existsSync('src/components/system/system-loader.jsx'), 'system-loader.jsx deleted'); console.log('OK')"</automated>
  </verify>
  <done>`system-hero.jsx` no longer imports or references `SystemLoader`/`BootSequence`, imports and mounts `SystemBoot` as an always-present sibling gated by `!booted`, declares `booted` state defaulting to `false` and a reduced-motion effect that sets it `true` immediately, wraps the top HUD bar + `TourCaption` + `HudHint` + `HudDock` behind `{booted && (...)}`, the `dynamic()` call for `SystemScene` no longer has a `loading` option, and `system-loader.jsx` no longer exists on disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure client-side UI/animation-sequencing fix; no new data input, no network calls, no user-supplied content. Existing trust boundaries (links data, identity data) are unchanged and untouched by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | DoS (minor) | `SystemBoot` timer cascade | accept | Timers are cleared on unmount via existing `useEffect` cleanup pattern (copied from original `BootSequence`); no untrusted input drives timer count or duration. |
| T-quick-02 | Tampering | N/A — no package installs in this plan | accept | No new dependencies added; only existing Framer Motion / React APIs already in use. |
</threat_model>

<verification>
1. Run `npm run dev` (or `next dev`), load `/` in a browser with motion enabled.
2. Confirm exactly ONE boot overlay appears (no double-flash): the glass card with pulsing core + `sourabh-jha://system` + scanning bar + the 4 progressive boot lines, ending on `> system online`.
3. Confirm the top HUD bar, tour caption area, "drag or click to explore" hint, and bottom nav dock (with Résumé button) are NOT visible and NOT clickable while the boot card is showing.
4. After the boot card fades out, confirm HUD bar / hint / dock appear and are clickable.
5. Click or press a key during boot — confirm it skips immediately to fade-out, then HUD appears.
6. Enable OS-level "reduce motion" and reload — confirm `SystemBoot` is skipped entirely and HUD chrome appears immediately (not stuck hidden).
7. Confirm `src/components/system/system-loader.jsx` no longer exists and the build has no dangling import errors: `npm run build` completes without "Module not found" for `system-loader`.
</verification>

<success_criteria>
- Single merged boot overlay replaces the two previous overlays (`SystemLoader` dynamic-import fallback + inline `BootSequence`).
- `src/components/system/system-loader.jsx` deleted; zero remaining references in the codebase.
- HUD chrome (top bar, tour caption, hint, dock) is gated behind `booted` and only becomes visible/clickable after the boot overlay's exit animation completes (via `AnimatePresence` `onExitComplete`).
- Reduced-motion path still shows HUD immediately, never blocked.
- `system-scene.jsx`, `system-fallback.jsx`, and data files unchanged.
- `npm run build` succeeds with no broken imports.
</success_criteria>

<output>
Create `.planning/quick/260617-2om-fix-boot-loading-sequence-merge-systemlo/260617-2om-SUMMARY.md` when done
</output>
