---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/data/README.md
  - src/data/identity.js
  - src/data/stats.js
  - src/data/projects.js
  - src/data/skills.js
  - src/data/experience.js
  - src/data/links.js
  - src/data/placeholders.js
  - src/app/page.js
  - src/components/motion/reveal.jsx
  - tests/data.test.mjs
autonomous: false
requirements: [FND-07, FND-04, MOT-01]
user_setup: []

must_haves:
  truths:
    - "Site content (identity, stats, projects, experience, skills, links) reads from editable in-repo data files seeded with known facts and clearly tagged TODO placeholders"
    - "Every TODO placeholder uses one consistent, greppable convention documented at the top of src/data/"
    - "The layout reflows cleanly across mobile, tablet, and desktop with no horizontal scroll"
    - "The shared section-reveal wrapper supports staggered child reveals and degrades under reduced motion"
  artifacts:
    - path: "src/data/README.md"
      provides: "Documents the placeholder convention (D-03) and the data-file architecture (D-01)"
      contains: "placeholder"
    - path: "src/data/stats.js"
      provides: "Stat seed data (Years of Experience, Projects Built, Technologies, Repositories, Open Source Contributions) with TODO placeholder values"
      contains: "isPlaceholder"
    - path: "src/data/projects.js"
      provides: "Seed project capsules (Git Automation CLI, DevOps Monitoring Dashboard, AEM Automation Toolkit, Cloud Infrastructure Projects) with placeholder detail fields"
      contains: "Git Automation CLI"
    - path: "src/data/skills.js"
      provides: "Seed technology constellation nodes (Golang, React, Next.js, Node.js, Docker, Kubernetes, AWS, Linux, AEM, Git, DevOps)"
      contains: "Kubernetes"
    - path: "src/data/experience.js"
      provides: "Mission-log experience entries with placeholder role/timeline/achievements"
      contains: "isPlaceholder"
    - path: "src/data/links.js"
      provides: "External links (GitHub, LinkedIn, email) seeded with known facts"
      contains: "github.com/jha-sk"
    - path: "src/data/placeholders.js"
      provides: "Shared placeholder helper/marker the convention is built on"
      exports: ["isPlaceholder", "TODO"]
  key_links:
    - from: "src/data/stats.js"
      to: "src/data/placeholders.js"
      via: "uses the shared placeholder marker"
      pattern: "placeholders"
    - from: "src/app/page.js"
      to: "src/data/stats.js"
      via: "placeholder section renders a stat using the empty-state copy when isPlaceholder"
      pattern: "from ['\"]@/data/"
---

<objective>
Complete the in-repo content data architecture and finish the responsive + motion baseline so every later phase has a stable surface to consume. Build the six per-domain data files (D-01) seeded with known facts and a single greppable placeholder convention (D-02/D-03), prove the responsive baseline reflows cleanly across breakpoints (FND-04), and harden the motion wrapper with staggered child reveals (MOT-01) — all demonstrated on the existing placeholder section.

Purpose: Phases 2-6 import `@/data/*` and reuse the motion wrapper; v2 (CNT2-01) fills placeholders by grepping one marker. This plan makes both trivial.
Output: `src/data/{identity,stats,projects,skills,experience,links}.js` + `placeholders.js` + `README.md`, an extended `Reveal` with stagger support, a responsive-proven placeholder page, and a data-shape test.
</objective>

## Phase Goal

**As a** recruiter or engineering manager visiting Sourabh Jha's portfolio, **I want to** load a running "Career OS" app whose foundation (tokens, theme toggle, data-driven content, motion) is live and reflows cleanly, **so that** every later section of the experience can be built on a premium, consistent base.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

# Binding contracts + the slice this builds on
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-design-system/01-SKELETON.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data architecture — six per-domain files + the greppable placeholder convention</name>
  <files>src/data/placeholders.js, src/data/README.md, src/data/identity.js, src/data/stats.js, src/data/projects.js, src/data/skills.js, src/data/experience.js, src/data/links.js, tests/data.test.mjs</files>
  <read_first>
    - .planning/phases/01-foundation-design-system/01-CONTEXT.md (D-01 split per-domain files; D-02 known facts vs placeholders — exact seed data; D-03 greppable placeholder convention — Claude's discretion on marker)
    - .planning/REQUIREMENTS.md (FND-07, and the field shapes implied by DASH-01, SKL-03, PRJ-05, EXP-02 that later phases consume)
    - src/data/identity.js (the identity file created in 01-01 — extend, do not duplicate)
  </read_first>
  <behavior>
    - Every src/data file default-exports (or named-exports) a plain JS object/array with stable, documented field names.
    - identity, links, skill names, and project names hold KNOWN facts (no placeholder marker).
    - Unknown values (stat numbers, repo/demo URLs, project detail fields, achievements, experience timeline) carry the single placeholder marker.
    - A single grep for the chosen marker token finds EVERY placeholder across all six files and nothing else.
    - tests/data.test.mjs asserts: all six files import without throwing; known seed facts are present (name "Sourabh Jha", links github.com/jha-sk + linkedin.com/in/sk-jha + codewithsourabhjha@gmail.com, the 11 seed skills, the 4 seed project names); and `isPlaceholder()` correctly flags marked values.
  </behavior>
  <action>
    Decide and document ONE greppable placeholder convention (D-03, Claude's discretion) — implement it in `src/data/placeholders.js` exporting both a `TODO(label)` factory that produces a marked value and an `isPlaceholder(value)` predicate (e.g. a value wrapper carrying a `__placeholder: true` flag plus a `TODO_`-prefixed string, or an equivalent single-token scheme). Document the convention and the per-domain architecture at the top of `src/data/README.md` so v2 (CNT2-01) can fill placeholders by grepping one token.
    Create the six per-domain files (D-01): extend `identity.js` (already has name/title/tagline from 01-01) if any fields are missing; `links.js` with KNOWN facts — GitHub `github.com/jha-sk`, LinkedIn `linkedin.com/in/sk-jha`, email `codewithsourabhjha@gmail.com`, resume path as a placeholder (PDF dropped into /public later); `stats.js` with the five labelled metrics (Years of Experience, Projects Built, Technologies, Repositories, Open Source Contributions) whose numeric VALUES are placeholders; `skills.js` with the 11 seed technologies (Golang, React, Next.js, Node.js, Docker, Kubernetes, AWS, Linux, AEM, Git, DevOps) as constellation nodes, with experience/relatedProjects/level/relatedTech fields as placeholders; `projects.js` with the 4 seed project names as known facts and Overview/Architecture/TechStack/Features/Challenges/LessonsLearned/githubUrl/demoUrl/screenshot fields as placeholders; `experience.js` with mission-log entries whose role/description/technologies/achievements/timeline are placeholders. Field names must cover what DASH-01, SKL-01/02/03, PRJ-01..05, EXP-01/02 will consume — pick stable shapes now (Claude's discretion on exact field names) and record them in README.md. Write `tests/data.test.mjs` (node:test) asserting the behaviors above. Add an npm script `test:data`.
  </action>
  <verify>
    <automated>cd c:/Projects/Portfolio && node --test tests/data.test.mjs && node -e "const fs=require('fs');const files=['identity','stats','projects','skills','experience','links'].map(f=>'src/data/'+f+'.js');files.forEach(f=>{if(!fs.existsSync(f)){console.error('missing',f);process.exit(1)}});const all=files.map(f=>fs.readFileSync(f,'utf8')).join('\n');['github.com/jha-sk','linkedin.com/in/sk-jha','codewithsourabhjha@gmail.com','Kubernetes','Git Automation CLI'].forEach(t=>{if(!all.includes(t)){console.error('missing seed fact',t);process.exit(1)}});console.log('data-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All six per-domain files plus placeholders.js and README.md exist under src/data/.
    - README.md documents the single placeholder convention and the field shapes for each file.
    - Known seed facts (identity name, the three links, 11 skills, 4 project names) are present and NOT marked as placeholders.
    - A single grep for the chosen marker token finds every placeholder and nothing else.
    - `node --test tests/data.test.mjs` passes.
  </acceptance_criteria>
  <done>Six per-domain data files, the documented greppable placeholder convention, and the data-shape test all exist and pass; known facts seeded, unknowns marked.</done>
</task>

<task type="auto">
  <name>Task 2: Stagger the motion wrapper + prove the responsive baseline on the placeholder section</name>
  <files>src/components/motion/reveal.jsx, src/app/page.js</files>
  <read_first>
    - .planning/phases/01-foundation-design-system/01-UI-SPEC.md (Motion Baseline — stagger 0.06s for grouped reveals; Responsive Baseline — gutters 16/24/32, breakpoints, max content width 1200px, no horizontal scroll >=320px)
    - src/components/motion/reveal.jsx (the wrapper from 01-01 — extend its contract, do not redefine the reveal timing)
    - src/data/stats.js (to render a small grouped, staggered demo using real data + empty-state copy)
  </read_first>
  <action>
    Extend `src/components/motion/reveal.jsx` to support staggered children (MOT-01, UI-SPEC: children stagger `0.06s` for grouped reveals) without changing the existing single-reveal contract — e.g. export the existing default `Reveal` plus a `RevealGroup`/`RevealItem` (or a `stagger` prop) using Framer Motion variants and `staggerChildren: 0.06`. Keep the `useReducedMotion()` gate: under reduced motion both single and grouped reveals render statically (no translate). Animate transform/opacity only.
    Update `src/app/page.js` so the placeholder section proves the responsive baseline (FND-04): wrap content in the 1200px max-width container with gutters `16px` mobile / `24px` (`md:`) / `32px` (`lg:`), and render a small grouped demo (e.g. the five `stats` labels in a 1-col -> `md:` 2-up -> `lg:` multi-column glass-card grid) using `RevealGroup` and showing the empty-state copy "Nothing here yet" / "This section is awaiting data. Content loads from the in-repo data files." for placeholder stat values (per the UI-SPEC Copywriting Contract). Mobile-first Tailwind prefixes only; no horizontal scroll at >=320px. Tokens only — no hex.
  </action>
  <verify>
    <automated>cd c:/Projects/Portfolio && rm -rf .next && npm run build && node -e "const fs=require('fs');const r=fs.readFileSync('src/components/motion/reveal.jsx','utf8');if(!r.includes('useReducedMotion')){console.error('reduced-motion gate lost');process.exit(1)}if(!/0\.06|staggerChildren/.test(r)){console.error('stagger missing');process.exit(1)}const p=fs.readFileSync('src/app/page.js','utf8');if(!/md:/.test(p)||!/lg:/.test(p)){console.error('responsive prefixes missing');process.exit(1)}if(!/max-w-\[1200px\]|max-w-6xl/.test(p)){console.error('container max-width missing');process.exit(1)}console.log('motion+responsive-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - reveal.jsx still exports the original single-reveal contract AND adds staggered-children support at 0.06s, all behind the useReducedMotion gate.
    - page.js wraps content in a 1200px-max container with 16/24/32 gutters via mobile-first prefixes and renders a grouped staggered demo from stats data.
    - Placeholder stat values render the exact empty-state copy from the UI-SPEC.
    - `npm run build` exits zero.
  </acceptance_criteria>
  <done>The motion wrapper supports reduced-motion-safe staggered group reveals and the placeholder section demonstrates a clean responsive reflow across mobile/tablet/desktop; production build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — responsive reflow + staggered reveal</name>
  <files>(none — verification only)</files>
  <action>Pause for the user to verify the responsive reflow and staggered reveal in a browser. Claude has already built the data architecture, stagger support, and responsive layout in Tasks 1-2; this checkpoint confirms the on-screen result across breakpoints and both themes before the phase completes. Present the verification steps below and wait for the resume signal.</action>
  <what-built>
    The placeholder home section now renders a grouped, staggered reveal of the five stat labels (each showing the "awaiting data" empty state, since values are placeholders) inside a responsive 1200px container, in both themes.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev`, open http://localhost:3000.
    2. Confirm the stat group reveals with a subtle staggered fade/slide (items appear in sequence, not all at once).
    3. Resize from desktop (>=1280px) -> tablet (~800px) -> mobile (~360px): confirm the grid goes multi-column -> 2-up -> single column with no horizontal scroll and consistent gutters.
    4. Toggle to light theme and repeat the resize check — layout and contrast must hold in both themes.
    5. Confirm placeholder stats show the "Nothing here yet" / "awaiting data" empty-state copy (values are intentionally not yet filled).
    6. (Optional) Enable OS reduce-motion, reload — the stat group appears statically with no stagger animation.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (stagger, reflow, theme, empty-state copy).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo data files -> public static site | Data files contain only public portfolio content (names, public profile links). No secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | src/data/*.js committed to a public repo | mitigate | Only public, intentional content is seeded (public GitHub/LinkedIn handles, a public contact email already published by the user). No keys/secrets/private data; resume PDF is a later, deliberately-added public asset. |
| T-02-02 | Tampering | placeholder marker leaking to production UI | mitigate | The empty-state copy path renders for placeholder values, so raw `TODO_` tokens never surface to visitors; data.test.mjs verifies the isPlaceholder predicate. |

No new dependencies installed this plan (reuses the 01-01 audited set). No user input, no server routes, no `dangerouslySetInnerHTML`.
</threat_model>

<verification>
- `node --test tests/data.test.mjs` passes (file imports, seed facts present, isPlaceholder works).
- A single grep for the placeholder marker finds every placeholder and nothing else.
- `npm run build` exits zero with the responsive/staggered placeholder section.
- Human checkpoint (Task 3) confirms staggered reveal + clean reflow in both themes + empty-state copy.
</verification>

<success_criteria>
- Site content reads from six editable in-repo per-domain data files seeded with known facts + one greppable TODO convention (FND-07, D-01/D-02/D-03).
- Layout reflows cleanly across mobile/tablet/desktop with no horizontal scroll (FND-04).
- The shared motion wrapper supports reduced-motion-safe staggered group reveals (MOT-01, completes the motion baseline).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md` when done.
</output>
