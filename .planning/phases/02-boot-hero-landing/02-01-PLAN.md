---
phase: 02-boot-hero-landing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hero/hero.jsx
  - src/components/hero/hero-background.jsx
  - src/components/hero/scroll-cue.jsx
  - src/components/motion/hero-reveal.jsx
  - src/app/page.js
autonomous: false
requirements: [HERO-01, HERO-02]
must_haves:
  truths:
    - "On the home route the hero displays the SOURABH JHA wordmark, the title, and the tagline read from identity.js (not hardcoded)"
    - "The hero fills the first viewport (min-h-[100svh]) as a centered vertical stack"
    - "The hero background renders an animated aurora, floating-light blobs, and a particle dot layer using transform/opacity only"
    - "A scroll-down cue bobs gently at the bottom of the hero"
    - "Under prefers-reduced-motion the background and scroll cue render static and the hero is fully visible"
    - "D-06: hero animated background is built with CSS/SVG + Framer Motion only (no canvas, no new dependencies) — aurora + floating-light + particle layers"
    - "D-10: a subtle animated scroll-down cue sits at the bottom of the hero as the entry affordance"
    - "D-11: hero is a full-viewport-height first section; the Phase-1 placeholder/stat content is removed/relocated so the home route leads with the hero"
  artifacts:
    - path: "src/components/hero/hero.jsx"
      provides: "Centered data-driven hero section (wordmark/title/tagline + background + scroll cue)"
      contains: "identity.name"
    - path: "src/components/hero/hero-background.jsx"
      provides: "Three-layer ambient background (aurora + blobs + particles), reduced-motion gated"
      contains: "useReducedMotion"
    - path: "src/components/hero/scroll-cue.jsx"
      provides: "Lucide ChevronDown bob cue with aria-label, reduced-motion gated"
      contains: "ChevronDown"
    - path: "src/components/motion/hero-reveal.jsx"
      provides: "Animate-on-mount staggered reveal variant (0.08s) reusing the inherited itemVariants timing"
    - path: "src/app/page.js"
      provides: "Home route leading with <Hero /> (Phase-1 placeholder removed)"
      contains: "Hero"
  key_links:
    - from: "src/components/hero/hero.jsx"
      to: "src/data/identity.js"
      via: "import { identity }"
      pattern: "identity\\.(name|title|tagline)"
    - from: "src/app/page.js"
      to: "src/components/hero/hero.jsx"
      via: "import { Hero }"
      pattern: "components/hero/hero"
    - from: "src/components/hero/hero-background.jsx"
      to: "bg-gradient-aurora token"
      via: "className"
      pattern: "bg-gradient-aurora"
---

<objective>
Deliver the first visible vertical slice of Phase 2: the premium animated HERO. After this plan, a visitor loading the home route sees a full-viewport centered hero — the "SOURABH JHA" wordmark, the "AI Engineer | Backend Engineer" title, and the building-scalable-systems tagline (all read from `identity.js`), sitting over a subtle animated aurora / floating-light / particle background, with a gentle scroll-down cue. The Phase-1 placeholder section is removed so the home route leads cleanly with the hero (the boot overlay in plan 02 will reveal *into* this hero).

Decisions covered: D-06 (background = CSS/SVG + Framer only, no new deps), D-08, D-09, D-10 (animated scroll-down cue), D-11 (hero is full-viewport; Phase-1 placeholder removed/relocated).

Purpose: HERO-01 (content) and HERO-02 (animated GPU-friendly background) are the "wow" payload of the phase. Building the hero first means plan 02's boot wipe has a real surface to uncover.
Output: `src/components/hero/{hero,hero-background,scroll-cue}.jsx`, `src/components/motion/hero-reveal.jsx`, and a restructured `src/app/page.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

# Phase contracts (binding)
@.planning/phases/02-boot-hero-landing/02-CONTEXT.md
@.planning/phases/02-boot-hero-landing/02-UI-SPEC.md
@.planning/phases/02-boot-hero-landing/02-PATTERNS.md
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md

# Analog source files to mirror (read_first per task)
@src/components/motion/reveal.jsx
@src/app/page.js
@src/components/layout/theme-toggle.jsx
@src/data/identity.js
</context>

## Phase Goal

**As a** recruiter or engineering manager landing on the portfolio, **I want to** be met with a premium, alive, full-screen hero that names Sourabh and his role over subtle ambient motion, **so that** my first reaction is "this is one of the most unique developer portfolios I've ever seen."

> Derived from the ROADMAP Phase 2 goal + success criteria 3 and 4. This is a visual/experiential phase; the "capability" is the visitor perceiving the hero. Each task below advances what the visitor can see on `/`.

## MVP Slice Tasks

After Task 1 the visitor sees the centered hero text on the void. After Task 2 that text sits over the live ambient background and a scroll cue. Each task adds a layer a real visitor can perceive — no horizontal "build all the X" tasks.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hero text stack + animate-on-mount reveal, wired into the home route</name>
  <read_first>
    - src/data/identity.js (the `{ name, title, tagline }` export — D-09, read not hardcode)
    - src/app/page.js (exact analog: section shell, container gutters `px-4 md:px-6 lg:px-8`, the `text-display font-semibold text-primary [text-shadow:var(--glow-accent)]` heading treatment on line 25, the `max-w-prose text-body text-muted-foreground` secondary-text pattern on line 28)
    - src/components/motion/reveal.jsx (the inherited `itemVariants` timing on lines 66-73 and `groupVariants` 0.06s stagger on lines 59-64 — DO NOT edit this file; the hero needs an animate-on-mount variant, not whileInView)
    - .planning/phases/02-boot-hero-landing/02-UI-SPEC.md § Hero Composition + § Typography (wordmark spec)
  </read_first>
  <files>src/components/hero/hero.jsx, src/components/motion/hero-reveal.jsx</files>
  <action>
    Create `src/components/motion/hero-reveal.jsx` — an animate-ON-MOUNT staggered reveal (the hero enters after the boot wipe, above the fold, so `whileInView` from reveal.jsx will not fire reliably). Export `HeroReveal` (group) and `HeroRevealItem` (item) that compose `motion.div` with `initial="hidden"` + `animate="visible"` (NOT whileInView). Reuse the inherited fade+16px-y / 0.5s / ease `[0.22,1,0.36,1]` item timing from reveal.jsx's `itemVariants`; set the group `staggerChildren` to 0.08s (UI-SPEC default, deliberately slightly longer than the inherited 0.06s — local to this file, do NOT modify reveal.jsx). Gate on `useReducedMotion()` exactly as reveal.jsx does: under reduced motion swap to static variants (opacity 0→1, 0.01s, no translate). Mark `'use client'`.

    Create `src/components/hero/hero.jsx` (may stay a server component since it only reads data + composes the client reveal). Import `{ identity }` from `@/data/identity` and read `identity.name` (render UPPERCASE as the wordmark), `identity.title`, `identity.tagline` — never hardcode the strings (D-09). Section shell: `relative overflow-hidden flex flex-col items-center justify-center min-h-[100svh]` with inherited container gutters (D-11 — full-viewport-height first section). Centered stack wrapped in `HeroReveal` with `HeroRevealItem` children in order wordmark → title → tagline:
      - Wordmark: Display role at the 700-weight hero exception — `font-sans font-bold uppercase text-foreground [text-shadow:var(--glow-accent)] tracking-[-0.02em] leading-[1.1]` with fluid size `text-[clamp(2.5rem,8vw,6rem)]` (D-08, UI-SPEC § Typography). Use the `[text-shadow:var(--glow-accent)]` token treatment from page.js line 25 (token, never hex).
      - Title: `text-heading font-semibold text-foreground` with gap `mt-4 lg:mt-6` (md/lg per UI-SPEC spacing).
      - Tagline: `text-body text-muted-foreground max-w-prose` with gap `mt-2 md:mt-4`.
    Leave a slot for the scroll cue (added in Task 2). Consume TOKENS only — no hardcoded hex, no new `dark:` utilities (WR-02 carry-over: components consume `text-foreground`/`text-muted-foreground`/`var(--glow-accent)` so both themes render correctly).

    Replace `src/app/page.js`: remove the Phase-1 `System initialized` Reveal block and the stat-card `RevealGroup` (lines 20-66) entirely (D-11 — DEFAULT per UI-SPEC § Hero Composition Data & structure — stat cards are Phase 3/DASH). Render `<Hero />` as the sole content (import from `@/components/hero/hero`). Drop now-unused imports (`stats`, `isPlaceholder`, `Reveal`/`RevealGroup`/`RevealItem` if unused).
  </action>
  <verify>
    <automated>cd "c:/Projects/Portfolio" && npx next lint --file src/components/hero/hero.jsx --file src/components/motion/hero-reveal.jsx --file src/app/page.js 2>&1 | grep -viE '^#' | grep -iE 'error' | grep -c . ; test "$(grep -c -- 'identity\.\(name\|title\|tagline\)' src/components/hero/hero.jsx)" -ge 3 && grep -q 'min-h-\[100svh\]' src/components/hero/hero.jsx && grep -q 'clamp(2.5rem,8vw,6rem)' src/components/hero/hero.jsx && ! grep -nE '#[0-9a-fA-F]{3,6}' src/components/hero/hero.jsx src/components/motion/hero-reveal.jsx && echo HERO_TEXT_OK</automated>
  </verify>
  <done>`HERO_TEXT_OK` prints and the lint error-count line is `0`. `hero.jsx` reads all three identity strings (≥3 references), uses `min-h-[100svh]`, the `clamp(2.5rem,8vw,6rem)` wordmark size, and contains zero hardcoded hex. `page.js` renders `<Hero />` and no longer renders the Phase-1 placeholder/stat cards.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ambient hero background (aurora + blobs + particles) and scroll cue, composed into the hero</name>
  <read_first>
    - src/components/motion/reveal.jsx (the `'use client'` + `useReducedMotion()` early-return-static idiom, lines 1-3 and 19-33; the `transition` ease shape on line 41)
    - src/components/layout/theme-toggle.jsx (Lucide glyph import + `aria-label` + token color, lines 4 and 34-38)
    - src/app/globals.css (confirm tokens: `--gradient-aurora`, `--glow-accent`, `--glow-soft`, `--background`, `--foreground`, `--muted-foreground`)
    - .planning/phases/02-boot-hero-landing/02-UI-SPEC.md § Hero Background + § Scroll Cue + § GPU/performance rules
  </read_first>
  <files>src/components/hero/hero-background.jsx, src/components/hero/scroll-cue.jsx, src/components/hero/hero.jsx</files>
  <action>
    Create `src/components/hero/hero-background.jsx` (`'use client'`) using CSS/SVG + Framer Motion only — no canvas, no new dependencies (D-06). Container: `absolute inset-0 -z-10 pointer-events-none` (must never intercept clicks/scroll — protects the boot-skip listeners from plan 02 and the scroll cue). Three stacked layers, ALL animating `transform`/`opacity` ONLY (D-07, FND-06 — never width/height/top/left/box-shadow/filter inside loops; set blur once):
      - (a) Aurora: one or two `motion.div` using the `bg-gradient-aurora` token class (the inherited `--gradient-aurora`, already accent-tinted at low opacity — do NOT author a new gradient or hex). Slow drift via `transform` translate/scale + gentle opacity breathing; `transition` `{ duration: 18, ease: [0.22,1,0.36,1], repeat: Infinity, repeatType: 'mirror' }`.
      - (b) Floating light: 3 large blurred `motion.div` (`blur-3xl`, accent-tinted via the aurora/`shadow-glow` token treatment, low opacity), positioned off-center so light pools behind the wordmark; each on an independent slow `transform` translate loop ~14-20s with different phases. `will-change: transform` sparingly on these.
      - (c) Particles: ~28 small (1-3px) absolutely-positioned `text-foreground`/accent dots at low opacity (≤~0.4) from a mapped array; each on a slow `transform` translateY/translateX drift + gentle opacity twinkle ~10-20s, staggered phases. Do NOT put `will-change` on all 28.
    Gate on `useReducedMotion()`: under reduced motion render all three layers STATIC (no drift/twinkle, fixed positions/opacity) but still looking like an intentional calm aurora-lit void (D-05/D-07).

    Create `src/components/hero/scroll-cue.jsx` (`'use client'`) — the subtle animated scroll-down cue entry affordance (D-10). Import `ChevronDown` from `lucide-react`. Render at `absolute bottom-12` horizontally centered inside the hero. Color `text-muted-foreground` at rest; accent glow on hover/focus via `drop-shadow`/`var(--glow-accent)` (token). Gentle vertical bob via `motion.div` `animate={prefersReducedMotion ? {} : { y: [0, 8, 0] }}` with `transition={{ duration: 1.8, ease: 'easeInOut', repeat: Infinity }}` (transform/opacity only). Carry `aria-label="Scroll to content"`. If made focusable/clickable (optional anchor to next section), ensure a ≥44×44px hit area (inherited minimum). Gate the bob on `useReducedMotion()` (static chevron under reduced motion, still visible as an affordance).

    Compose both into `src/components/hero/hero.jsx`: mount `<HeroBackground />` behind the centered stack (it is `-z-10`/absolute) and `<ScrollCue />` pinned at the section bottom. Keep the section `relative overflow-hidden`. Tokens only — no hex, no new `dark:` utilities.
  </action>
  <verify>
    <automated>cd "c:/Projects/Portfolio" && grep -q 'bg-gradient-aurora' src/components/hero/hero-background.jsx && grep -q 'useReducedMotion' src/components/hero/hero-background.jsx && grep -q 'pointer-events-none' src/components/hero/hero-background.jsx && grep -q 'ChevronDown' src/components/hero/scroll-cue.jsx && grep -q 'aria-label="Scroll to content"' src/components/hero/scroll-cue.jsx && grep -q 'useReducedMotion' src/components/hero/scroll-cue.jsx && ! grep -nE '#[0-9a-fA-F]{3,6}' src/components/hero/hero-background.jsx src/components/hero/scroll-cue.jsx && grep -q 'HeroBackground' src/components/hero/hero.jsx && grep -q 'ScrollCue' src/components/hero/hero.jsx && npx next build 2>&1 | grep -iE 'failed|error' | grep -viE 'no err|0 error' | grep -c . | grep -q '^0$' && echo BG_CUE_OK</automated>
  </verify>
  <done>`BG_CUE_OK` prints. Background uses `bg-gradient-aurora` + `useReducedMotion` + `pointer-events-none`; scroll cue uses `ChevronDown` + the `aria-label` + `useReducedMotion`; neither file has hardcoded hex; the hero composes both; and `next build` completes without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — hero renders premium, animated, and reduced-motion safe in both themes</name>
  <what-built>The full hero is live on the home route: centered SOURABH JHA wordmark + title + tagline (from identity.js) with an animate-on-mount staggered entrance, a subtle aurora/floating-light/particle background, and a bobbing scroll cue. (The boot overlay arrives in plan 02 — for now the hero shows immediately on load.)</what-built>
  <how-to-verify>
    1. Run `cd "c:/Projects/Portfolio" && npm run dev` and open http://localhost:3000 (if a stale build misbehaves: kill the port owner, delete `.next`, rerun — PROJECT.md env note).
    2. Confirm the hero fills the viewport and is vertically/horizontally centered. "SOURABH JHA" reads as a large premium uppercase wordmark with a subtle ice-blue glow; below it the title "AI Engineer | Backend Engineer" and the tagline "Building scalable systems — backend foundations and AI on top." The wordmark stays on one line at desktop and ≥360px width (acceptable to wrap to two centered lines on the very smallest screens).
    3. Watch the background: aurora glow + a few large soft light blooms drift slowly, ~28 faint particles twinkle/drift. It must read as SUBTLE and premium (Apple-Vision-Pro calm) — NOT neon/cyberpunk, no saturated blue. The scroll cue chevron bobs gently at the bottom.
    4. Toggle the theme (top-right Sun/Moon). Confirm the hero, glow, and background look correct and intentional in BOTH dark and light — no wrong-theme flash, no invisible text.
    5. Enable reduced motion (OS setting or DevTools → Rendering → "Emulate prefers-reduced-motion: reduce") and reload. The hero must be fully visible immediately, the background static (calm, intentional — not broken), and the scroll-cue bob stopped.
    6. Resize across mobile/tablet/desktop widths — no horizontal scroll at ≥320px, stack stays centered.
  </how-to-verify>
  <resume-signal>Type "approved" if the hero is premium/subtle/centered, correct in both themes, and static under reduced motion — or describe what looks off (e.g. background too strong, wordmark overflows, wrong-theme flash).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← static asset host (Vercel) | Visitor loads a fully static, public, read-only page. No user input, no auth, no backend, no DB in this phase. |
| build ← dependencies | Hero is built from already-installed deps only (Framer Motion, lucide-react). D-06 mandates NO new dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npm/pip/cargo installs | mitigate | D-06 forbids new dependencies — hero uses only Framer Motion + lucide-react, both installed and vetted in Phase 1. No install task in this plan; if any executor reaches for a new package, stop. |
| T-02-02 | Information Disclosure | hardcoded secrets | mitigate | Static frontend; no env vars, no API keys. Verify gate greps confirm no stray secrets/hex. Content reads from `identity.js` (public facts only). |
| T-02-03 | Denial of Service | client render perf (animation) | accept | Background motion is transform/opacity-only, ~28 particles + 3 blobs + aurora — bounded node count, GPU-friendly per UI-SPEC. Reduced-motion renders static. Formal Lighthouse audit is Phase 7; risk is low (self-DoS only). |
</threat_model>

<verification>
- `next build` completes with no errors (Task 2 gate).
- `next lint` reports no errors on the new/changed files (Task 1 gate).
- No hardcoded hex in any new component (grep gates).
- All three `identity` strings are read, not hardcoded (grep gate).
- Background and scroll cue are reduced-motion gated (`useReducedMotion` present).
- Human checkpoint confirms premium/subtle look, both-theme correctness, and reduced-motion static rendering (cannot be asserted headlessly).
</verification>

<success_criteria>
- HERO-01: Home route hero shows "SOURABH JHA", "AI Engineer | Backend Engineer", and the tagline, all from `identity.js`.
- HERO-02: Hero background renders animated particles, aurora gradients, and floating light with subtle, transform/opacity-only motion; static under reduced motion.
- D-11: Hero is the full-viewport leading section; Phase-1 placeholder removed.
- Both themes render correctly (WR-02 carry-over respected: tokens only, no new `dark:` utilities, no hex).
</success_criteria>

<output>
Create `.planning/phases/02-boot-hero-landing/02-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
