# ── Contact form (Resend) ─────────────────────────────────────────────────
# Copy this file to `.env.local` and fill in your values, then restart `npm run dev`.

# Required: your Resend API key (https://resend.com → API Keys). Server-only secret.
RESEND_API_KEY=re_CPaEcvnD_G7FWdw1iaqv4pfaBryLWg39e
# Optional: verified sender. Until you verify a domain in Resend, leave this unset
# to use Resend's shared test sender (onboarding@resend.dev), which can only
# deliver to the email address on your Resend account.
# For production: RESEND_FROM="Career OS <noreply@yourdomain.com>"
# RESEND_FROM=

# Optional: where messages are delivered. Defaults to the email in src/data/links.js.
# CONTACT_TO=codewithsourabhjha@gmail.com
